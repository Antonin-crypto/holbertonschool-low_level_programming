#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Succes)
 * /
 * int main (void)
 * {
 * printf("Size of a char:%lu byte(s)\n";size of (char));
 * printf("size of an int:%lu byte(s)\n";size of (int));
 * printf("size of a long int:%lu byte (s)\n";size of(long long int));
 * printf("size of a floot:\n";size of (floot));
 * return(0);
 *}
